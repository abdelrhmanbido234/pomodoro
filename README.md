
  # Pomodoro Timer Mobile UI

  This is a code bundle for Pomodoro Timer Mobile UI. The original project is available at https://www.figma.com/design/pFVg209KN8fgm78ViuhYBl/Pomodoro-Timer-Mobile-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  